type: boolean
description: >
  Due to delays in synchronization between Oneproviders, the number of returned
  entries can be smaller that the requested limit, even though there are still
  some ensuing ones.
  The `isLast` flag is the only valid indicator that all entries has been listed.
example: true
