type: object
description: The list of views names.
properties:
  views:
    type: array
    description: List of space views
    items:
      type: string
  nextPageToken:
    $ref: '#/definitions/ListPageToken'
required:
  - views
  # NOTE: nextPageToken cannot be set as required as it can have null value,
  # which causes the generated API client to fail response validation
  # (this workaround is needed due to Swagger 2.0 limitations)
example:
  views:
    - images
    - videos
  nextPageToken: 8471726779817b3a
