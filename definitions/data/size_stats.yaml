type: object
description: | 
  Represents size statistics for the directory. Returns statistics based on the chosen mode of operation.
  If mode is layout than provides a layout structure.
  If mode is slice than describes statistics for slices of data.
properties:
    layout:
      $ref: '#/definitions/TimeSeriesLayout'
    slice:
      $ref: '#/definitions/TimeSeriesSlice'
example:
  layout: 
    total_size: 
      - day
      - hour
      - minute
      - month
    storage_use_7ab46e96fdb27a2694493c2c5bd7491dch8f55:
      - day
      - hour
      - minute
      - month
    reg_file_and_link_count:
      - day
      - hour
      - minute
      - month
    file_errors_count:
      - day
      - hour
      - minute
      - month
    dir_errors_count:
      - day
      - hour
      - minute
      - month
    dir_count:
      - day
      - hour
      - minute
      - month
