type: string
description: |
  The type of the file:
  * `REG` - regular file.
  * `DIR` - directory.
  * `LNK` - hardlink.
  * `SYMLNK` - symbolic link.
enum:
  - REG
  - DIR
  - LNK
  - SYMLNK
example: REG
