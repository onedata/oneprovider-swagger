type: string
description: |
  The state of the archive:

  * `empty` - the archive metadata has been created in the database but the process of archiving
  data on the storage has not started yet,

  * `initializing` - the process of archiving data on the storage is in progress,

  * `persisted` - data has been successfully archived on the storage,

  * `failed` - an error occured during process of archiving data on the storage,

  * `purging` - archived data is being purged from the storage. When archive is purged, all its metadata will be deleted,
  therefore it will be impossible to fetch it.
enum:
  - empty
  - initializing
  - archived
  - failed
  - purging
example: archived
