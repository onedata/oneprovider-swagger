type: string
description: |
  The structure in which the metadata is stored in the archive:

  * `builtIn` - Metadata are stored only in the database of Oneprovider,
  they are not stored alongside directories and files on the storage.

  * `json` - TODO

  * `xml` - TODO
enum:
  - builtIn
  - json
  - xml
example: json
