get:
  operationId: get_file_qos_by_id
  x-onedata-gri:
    type: op_qos
    id: ?OBJECTID_BINDING(id)
    aspect: effective_qos
  summary: Get effective QoS by id
  tags:
    - QoS
  x-bash-codegen-description: |
    Returns effective QoS for file or directory specified by ID.
  description: |
    Returns effective QoS for file or directory specified by ID.

    ***Example cURL requests***

    **Get effective QoS for file specified by ID**
    ```bash
    curl --tlsv1.2 -X GET -H "X-Auth-Token: $TOKEN" \
    "https://$HOST/api/v3/oneprovider/qos-id/$FILE_ID"

    {
      "targetStorages": {"6969ba86d2172e15f8156ccb32a58b92": ["fc05cfc92eda7a054b835bc1ee4a1372"]},
      "qosList": ["fc05cfc92eda7a054b835bc1ee4a1372"],
      "fulfilled": false
    }

    ```
  produces:
    - application/json
  responses:
    '200':
      description: Effective QoS for file or directory.
      schema:
        $ref: '#/definitions/EffectiveQos'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Forbidden request.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: File not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
    - name: id
      in: path
      description: Id of the file.
      type: string
      required: true
  x-code-samples:
    - lang: Shell
      source: "oneprovider-rest-cli getFileQosById id=$FILE_ID"


post:
  operationId: add_qos_by_id
  x-onedata-parse_body: as_json_params
  x-onedata-gri:
    type: op_qos
    id: ?OBJECTID_BINDING(id)
    aspect: instance
  summary: Add QoS entry by id
  tags:
    - QoS
  x-bash-codegen-description: |
    Adds QoS entry for file or directory specified by ID.
  description: |
    Adds QoS entry for file or directory specified by ID.

    ***Example cURL requests***

    **Add QoS for file specified by ID**
    ```bash
    curl --tlsv1.2 -H "X-Auth-Token: $TOKEN" -X POST \
    -H "Content-type: application/json" \
    -d '{ "expression": "country=FR", "replicasNum": 2 }' \
    "https://$HOST/api/v3/oneprovider/qos-id/$FILE_ID"
    ```
  consumes:
    - application/json
  responses:
    '200':
      description: Id of added QoS entry (e.g. 'fc05cfc92eda7a054b835bc1ee4a1372')
      schema:
        type: string
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Forbidden request.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: File not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
    - name: id
      in: path
      description: Id of the file.
      type: string
      required: true
    - name: data
      in: body
      description: Data containig expression and required replicas number.
      schema:
        $ref: '#/definitions/QosReq'
      required: true
  x-code-samples:
    - lang: Shell
      source: "oneprovider-rest-cli addQoSById id=$FILE_ID expression='country=FR' replicasNum=2"
