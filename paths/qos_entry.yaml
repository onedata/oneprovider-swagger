get:
  operationId: get_qos
  x-onedata-gri:
    type: op_qos
    id: ?BINDING(qid)
    aspect: qos
  summary: Get QoS details
  tags:
    - QoS
  x-bash-codegen-description: |
    Returns detailed information about particular QoS by its ID.
  description: |
    Returns detailed information about particular QoS by its ID.

    ***Example cURL requests***

    **Get detailed information about QoS**
    ```bash
    curl --tlsv1.2 -X GET -H "X-Auth-Token: $TOKEN" \
    "https://$HOST/api/v3/oneprovider/qos-entry/c84f669f9522c46976fee490d80651f0"

    {
      "qosId": "c84f669f9522c46976fee490d80651f0",
      "qosExpression": "country=FR",
      "replicasNum": 2,
      "fulfilled": true
    }

    ```
  produces:
    - application/json
  responses:
    '200':
      description:
      schema:
        $ref: '#/definitions/Qos'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Forbidden request.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Oneprovider REST API not available.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
    - name: qid
      in: path
      description: QoS ID (e.g. 'c84f669f9522c46976fee490d80651f0')
      type: string
      required: true
  x-code-samples:
    - lang: Shell
      source: "oneprovider-rest-cli getQos"


delete:
  operationId: remove_qos
  x-onedata-gri:
    type: op_qos
    id: ?BINDING(qid)
    aspect: qos
  summary: Remove QoS
  tags:
    - QoS
  x-bash-codegen-description: |
    Removes QoS requirement.
  description: |
    Removes QoS requirement.

    ***Example cURL requests***

    **Remove QoS.**
    ```bash
    curl --tlsv1.2 -H "X-Auth-Token: $TOKEN" -X DELETE \
    -H "Content-type: application/json" \
    "https://$HOST/api/v3/oneprovider/qos-entry/c84f669f9522c46976fee490d80651f0"
    ```
  consumes:
    - application/json
  responses:
    '204':
      description: QoS requirement has been deleted.
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Forbidden request.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: File not found.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
    - name: qid
      in: path
      description: QoS ID (e.g. 'c84f669f9522c46976fee490d80651f0')
      type: string
      required: true
  x-code-samples:
    - lang: Shell
      source: "oneprovider-rest-cli removeQos qid='c84f669f9522c46976fee490d80651f0'"
