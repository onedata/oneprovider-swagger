get:
  operationId: get_size_stats_collection_schema
  x-onedata-gri:
    type: op_file
    aspect: dir_size_stats_collection_schema
    scope: public
  summary: Get size statistics collection schema.
  tags:
    - Basic File Operations
  x-bash-codegen-description: |
    This method returns a collection of time series schemas, 
    which define the structure and configuration of time series data.
  description: |
    This method returns a collection of time series schemas, 
    which define the structure and configuration of time series data.
    
    ***Example cURL requests***
    
    **Get size statistics collection schema**
    ```bash
      curl -X GET "https://$PROVIDER_HOST/api/v3/oneprovider/data/size_stats_collection_schema"
    
    {
      "timeSeriesSchemas": [
        {
          "nameGeneratorType": "exact",
          "nameGenerator": "total_size",
          "unit": "bytes",
          "metrics": {
           "month":{
               "retention":360,
               "resolution":2592000,
               "aggregator":"last"
            },
            "minute":{
               "retention":720,
               "resolution":60,
               "aggregator":"last"
            },
            "hour":{
               "retention":1440,
               "resolution":3600,
               "aggregator":"last"
            },
            "day":{
               "retention":550,
               "resolution":86400,
               "aggregator":"last"
            }
          }
        },
        {
         "nameGeneratorType":"exact",
         "nameGenerator":"dir_count",
         "unit":"none",
         "metrics":{
            "month":{
               "retention":360,
               "resolution":2592000,
               "aggregator":"last"
            },
            "minute":{
               "retention":720,
               "resolution":60,
               "aggregator":"last"
            },
            "hour":{
               "retention":1440,
               "resolution":3600,
               "aggregator":"last"
            },
            "day":{
               "retention":550,
               "resolution":86400,
               "aggregator":"last"
            }
         }
      },
      ]
    }
    ```
  produces:
    - application/json
  responses:
    '200':
      description: Returns the requested size statistics collection schema.
      schema:
        $ref: '#/definitions/TimeSeriesCollection'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server error.
      schema:
        $ref: '#/definitions/Error'
  x-code-samples:
    - lang: Shell
      source: "oneprovider-rest-cli getSizeStatsCollectionSchema"