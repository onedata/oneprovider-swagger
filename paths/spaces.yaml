get:
  operationId: get_all_spaces
  x-onedata-gri:
    type: op_space
    aspect: list
  tags:
    - Space
  summary: Get all user spaces
  x-bash-codegen-description: |
    Returns the list of all user spaces.
  description: |
    Returns the list of all user spaces.

    ***Example cURL requests***

    **List all user spaces**
    ```bash
    curl -X GET -H "X-Auth-Token: $TOKEN" "https://$HOST/api/v3/oneprovider/spaces"

    [
        {
            "spaceId": "fb519d81146bcc635b890ff03a5da0fdch34fe",
            "name": "Space1"
        },
        {
            "spaceId": "e14039c53235c44f2f25dce4c90b1f0acha61c",
            "name": "Space2"
        }
    ]
    ```
  produces:
    - application/json
  responses:
    '200':
      description: The list of all user spaces - containining their names and Id's.
      schema:
        type: array
        items:
          type: object
          properties:
              name:
                type: string
                description: Space name.
              spaceId:
                type: string
                description: Space Id.
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Forbidden request.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Oneprovider REST API not available.
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server error.
      schema:
        $ref: '#/definitions/Error'
  x-code-samples:
  - lang: Shell
    source: "oneprovider-rest-cli getAllSpaces"
